[build-system]
requires = ["setuptools", "wheel", "cython", "numpy>=1.22.0,<1.27.0"]
build-backend = "setuptools.build_meta"

[flake8]
max-line-length=120

[tool.black]
line-length = 120
target-version = ['py39']

[tool.isort]
line_length = 120
profile = "black"
multi_line_output = 3
